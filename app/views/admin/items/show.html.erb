<h3 class="header-font">Item <%= @item.name %></h1>

<div class="jumbotron table-responsive">
  <table class="table table-hover table-bticketed table-condensed table-font">
    <tr> <th class="center-content">Name</th> <th class="center-content">Price</th><th></th><th></th></tr>
      <tr>
        <td><%= @item.name %></td>
        <td><%= @item.price %></td>
        <td><%= image_tag @item.avatar.url(:square) %></td>
        <% if @item.retired == "N" %>
          <td><%= button_to "Add to Ticket", create_ticket_path(item_id: @item.id), class: "btn btn-success" %></td>
        <% else %>
          <td>Item Retired</td>
       <% end %>
      </tr>
  </table>
</div>
